#!/bin/bash
rebuild()
{
    (
        set -x
        set -e
        git clean -dxf .
        bash -x bin/install-npm-deps.sh
        ./gen-helpers
        gmake fastrender
        gmake PROD=1 fastrender
        gmake PROD=1 all
        gmake test
    )
}
rebuild
