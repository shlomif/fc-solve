#!/bin/bash
rebuild()
{
    (
        set -x
        set -e
        time git clean -dxf .
        time bash -x bin/install-npm-deps.sh
        time ./gen-helpers
        time gmake fastrender
        time gmake PROD=1 fastrender
        time gmake PROD=1 all
        time gmake test
    )
}
rebuild
